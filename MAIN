<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QUIQASSISTE</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      background: #f0f4f8;
      padding: 2rem;
    }
    .container {
      background: #fff;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 400px;
      text-align: center;
    }
    input, button {
      padding: 0.6rem 1rem;
      margin: 0.5rem 0;
      border-radius: 8px;
      border: 1px solid #ccc;
      width: 80%;
      font-size: 1rem;
    }
    button {
      cursor: pointer;
      background: #4caf50;
      color: white;
      border: none;
      transition: 0.3s;
    }
    button:hover {
      background: #45a049;
    }
    .log {
      margin-top: 1rem;
      text-align: left;
    }
    .log-item {
      background: #f1f1f1;
      padding: 0.5rem;
      margin-bottom: 0.5rem;
      border-radius: 6px;
      font-size: 0.9rem;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>QUIQASSISTE</h1>
    <input id="userInput" type="text" placeholder="Type your message..." />
    <br>
    <button onclick="addEntry()">Submit</button>

    <div class="log" id="log"></div>
  </div>

  <script>
    const log = document.getElementById("log");

    function addEntry() {
      const input = document.getElementById("userInput").value.trim();
      if (!input) return alert("Please type something!");

      const timestamp = new Date().toLocaleString();
      const entry = document.createElement("div");
      entry.className = "log-item";
      entry.textContent = `[${timestamp}] ${input}`;
      log.prepend(entry); // adds new entries at the top

      document.getElementById("userInput").value = "";
    }
  </script>
</body>
</html>

